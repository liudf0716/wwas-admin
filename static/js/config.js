// 运行时配置文件
// 这个文件可以在部署时根据不同环境进行修改
window.APP_CONFIG = {
  // API 基础地址配置
  // 如果为空，则使用相对路径 '/api'
  // 如果需要指向其他服务器，可以设置完整的 URL
  API_BASE_URL: '',
  
  // 其他运行时配置
  // VERSION: '1.0.0',
  // ENVIRONMENT: 'production'
};
